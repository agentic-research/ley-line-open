[package]
name = "leyline-public-schema"
version = "0.1.0"
edition = "2024"

[dependencies]
prost = "0.12"
serde = { version = "1", features = ["derive"] }
tonic = "0.11"

[build-dependencies]
prost-build = "0.12"
tonic-build = "0.11"
