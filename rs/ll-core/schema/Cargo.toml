[package]
name = "leyline-schema"
version = "0.10.2"
edition = "2024"
license = "Apache-2.0"

[dependencies]
rusqlite = { workspace = true, features = ["bundled", "serialize"] }
anyhow = { workspace = true }
