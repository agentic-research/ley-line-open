[package]
name = "leyline-schema"
version = "0.10.1"
edition = "2024"
license = "AGPL-3.0-or-later"

[dependencies]
rusqlite = { workspace = true, features = ["bundled", "serialize"] }
anyhow = { workspace = true }
